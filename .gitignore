# --- MARKOV SHIELD v150 GITIGNORE ---

# Python & System
__pycache__/
*.py[cod]
*$py.class
.DS_Store
.vscode/
.vs/

# CUDA/C++ Временные файлы (кроме самих ядер)
*.o
*.obj
*.cu.o
*.tmp
*.log

# Временные файлы весов (чтобы не забить Гитхаб гигабайтами)
temp_*.bin
model.safetensors

# ПАПКИ СБОРКИ (Разрешаем dist для артефактов)
# Мы НЕ игнорируем *.exe, чтобы Гитхаб мог их выгрузить в релиз
# Мы НЕ игнорируем dist/ во время сборки GitHub Actions

# Игнорируем только тяжелые локальные папки IDE
bin/
obj/
out/
CMakeSettings.json
